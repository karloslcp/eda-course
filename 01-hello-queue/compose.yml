name: task-01_hello-queue

services:
  rabbitmq:
    image: rabbitmq:4-management-alpine
    container_name: eda-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest